<div class="container mt-5 text-center">
  <h1>🚧 Catálogo en construcción</h1>
  <p>Estamos trabajando para habilitar esta sección.</p>
  <% if (typeof vistaSolicitada !== 'undefined') { %>
    <p class="text-muted">Vista solicitada: <code><%= vistaSolicitada %></code></p>
  <% } %>
</div>